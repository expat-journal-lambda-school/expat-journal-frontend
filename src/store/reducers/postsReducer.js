const initialState = {
  posts: [],
  isLoading: false,
  errorMessage: null,
  isLoggedIn: false
}

export const postsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
